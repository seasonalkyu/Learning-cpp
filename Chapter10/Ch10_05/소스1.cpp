// Integer가 담기는 IntArray 클래스 직접 구현하기

class IntArray
{
private:
	int m_length = 0;
	int* m_data = nullptr;

public:
	//Constructors
	// Destructors
	// initialize()
	// reset()
	// resize()
	// insertBefore(const int & value, const int & ix);
	// remove(const int & ix);
	// push_back(const int& value);
};

int main()
{
	//IntArray my_arr{ 1,3,5,7,9 };
	//my_arr.insertBefore(10, 1);
	//my_arr.remove(3);
	//my_arr.push_back(13);
}



// 컨테이너 클래스
// std::vector<int> int_vec;
// std::array<int, 10> int_arr;
// vector, array, map, pair... 다양한 컨테이너 클래스가 존재한다
